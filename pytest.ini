[pytest]
testpaths = tests
addopts = -q
; Live logging, so passing tests report their numbers too. Without this pytest
; only replays captured output for failures and the Testing panel stays blank.
log_cli = true
log_cli_level = INFO
log_cli_format = %(message)s
filterwarnings =
    ignore::DeprecationWarning
